add compilation of a .dw into a .cdw file (optimize module and serialize)
add flag support to dawn, example: disable optimizations
optimize access operator
allow overloaded operators to be called even when order is not "Struct" "operator" "something"
