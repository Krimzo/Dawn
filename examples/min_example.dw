module min_example;

let a = 12 % (3 * (5)) ^ -(5.0 / 2.) * -(5 * 2);

internal {
	var b: float = 6;
	let c = '\\';
	let s = "12 % (3 * (5)) ^ -(5.0 / 2.) * -(5 * 2)";
}

func main(args: string[]) {
	print(a + b * 2);
}
