func main() {
	let animals = [Animal{mouse}, Animal{cat}, Animal{dog}]
	for ref animal : animals {
		print(animal, " = ", animal->value)
	}
}

enum Animal {
	mouse = "Mouse",
	cat = 'C',
	dog = "woof " + "woof",
}
