import "dev_libs.dw"

func main(array& args)
{
	string? a = "some nice text"
	print(a)
	offset_string(a, 5)
	print(a)
	offset_string(a, -5)
	print(a)
}

func offset_string(string& str, int& offset)
{
	for c : str {
		c = char(int(c) + offset)
	}
}
