name: Faxdawn
type: intercompiled

source extension: .fxdn
compiled extension: .fxex

compile command: fxdc SourceFile.fxdn
execute command: fxdn CompiledFile.fxex

line end		;
code scope		{}
splitter		()
arrays			[]
comment			$$

keywords:
if, elif, else,
not, and, or,
switch, case,
for, while, do, loop, break, continue,
def, return,
class, pub, self,
use

primitives:
bool, byte, char, int(64bit), float(64bit)

built-in:
string,
lambda

function example
Test() -> int {
	return 6;
}

loading modules:
use module_name.fxdn;

std lib:
file management (image, audio, video)
gui (CPU, DirectX, OpenGL)
web (html parsing, file downloading)
tcp (server - client)
multithreading (mutex, atomic)
