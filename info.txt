name: Faxdawn
type: interpreted
extension: .xdn

interpreter: faxdawn filename.xdn

syntax:
command end		!
dereference		?
code snippet	[]
splitter		()
comment			$$

bools(same as c++):
equal			==
not equal		!=
greater			>
smaller			<
...

built in functions(+math stuff):
out, outsl, for, while, do while, if, else if, else

for (int i = 0) (i? < 5) (i?++) [
	if (i <= 2) [
		out(i?)!
	]
]

variables: bol, int(int64), flt(double), str
simple arithmetic
custom functions
arrays

built in:
file management(bitmap, audio, video)
gui (CPU, DirectX)
web (html parsing, file downloading)
tcp (server - client)
multithreading (mutex, atomic)
